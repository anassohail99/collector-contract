[profile.default]
src = 'src'
out = 'out'
libs = ['lib']

# See more config options https://github.com/foundry-rs/foundry/tree/master/config

[rpc_endpoints]
testnetBSC = "${TBSC_RPC_URL}"

[etherscan]
testnetBSC = { key = "${BSCSCAN_API_KEY}" }